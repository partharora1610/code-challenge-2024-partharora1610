/**
 * Size fo the block head in weight units
 */
export const BLOCK_HEADER_SIZE = 320;

/**
 * Size of coinbase transaction in weight units
 */
export const COINBASE_TX_SIZE = 100;

/**
 * Coinbase reward to the miner
 */
export const COINBASE_REWARD = 6.25 * 1e8;
